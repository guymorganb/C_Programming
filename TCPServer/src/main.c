#include <stdio.h> // Includes the Standard Input Output library

int main()
{
    printf("Hello, World!\n"); // Prints "Hello, World!" to the console
    return 0;                  // A return value of 0 indicates that the program ran successfully
}